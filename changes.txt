
2022-02-25 0.1.3:
 - new 任务消息队列由插件移至 kernel 模块。
 - new 配置文件 syscfg 中增加 local_cpu_thread 本机处理器逻辑核心数。

2022-02-15 0.1.2:
 - new 模块 cyberx_js 和 cyberx_py 增加 GetKernel() 用于获取核心模块实例。

2022-02-09 0.1.1:
 - new 完善 cyberx_js 和 cyberx_py 模块。
 - new 完成本地直接任务执行实例。
 - new 完成本地普通任务同步执行实例。
 - new 完成本地普通任务异步执行实例。

2022-01-30 0.1.0:
 - new 项目构建。
 - new 增加 syscfg 系统参数配置模块。
 - new 模块 master 和 worker 构建。
 - new 增加 syslog 系统日志处理模块。
 - new 模块 kernel 构建。
 - new 模块 cyberx_js 构建。
 - new 模块 cyberx_py 构建。
 - new 增加 plugin 系统插件管理模块。
 - new 增加计算任务参数数据传输机制。
 - new 增加计算任务调用异步返回机制。
 - new 模块 matlib 构建。
 - new 移除 syslog 和 plugin 模块，适配 FrameX 框架。
 - new 设计 tasker 任务结构。
 - new 完成本地普通任务下达、执行、返回等调用路径测试。
 - new 完成本地直接任务执行调用路径测试。
